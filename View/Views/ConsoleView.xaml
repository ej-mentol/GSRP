<UserControl x:Class="GSRP.View.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:GSRP.Converters"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:EqualityToVisibilityConverter x:Key="EqualityToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid Background="#1e1f23" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#141518" 
                    BorderBrush="#2d2e33" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Margin="0,0,0,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              Padding="12"
                              Style="{StaticResource {x:Type ScrollViewer}}">
                    <ItemsControl ItemsSource="{Binding FilteredConsoleOutput}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Text}" 
                                           Foreground="#f2f2f2" 
                                           FontFamily="Consolas" 
                                           FontSize="12" 
                                           Margin="0,1" 
                                           TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="ConsoleInputTextBox"
                         KeyDown="ConsoleInput_KeyDown"
                         Grid.Column="0" 
                         Text="{Binding ConsoleInputText, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,8,0">
                    <TextBox.ContextMenu>
                        <ContextMenu Style="{StaticResource ThemedContextMenu}">
                            <MenuItem Command="ApplicationCommands.Cut" Header="Cut" Style="{StaticResource ThemedMenuItem}">
                                <MenuItem.Icon>
                                    <Border Background="#ef4444" CornerRadius="3" Width="16" Height="16">
                                        <TextBlock Text="✂️" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="ApplicationCommands.Copy" Header="Copy" Style="{StaticResource ThemedMenuItem}">
                                <MenuItem.Icon>
                                    <Border Background="#3b82f6" CornerRadius="3" Width="16" Height="16">
                                        <TextBlock Text="📋" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="ApplicationCommands.Paste" Header="Paste" Style="{StaticResource ThemedMenuItem}">
                                <MenuItem.Icon>
                                    <Border Background="#10b981" CornerRadius="3" Width="16" Height="16">
                                        <TextBlock Text="📄" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Command="ApplicationCommands.SelectAll" Header="Select All" Style="{StaticResource ThemedMenuItem}">
                                <MenuItem.Icon>
                                    <Border Background="#60a5fa" CornerRadius="3" Width="16" Height="16">
                                        <ui:FontIcon Glyph="&#xE8B3;" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Command="ApplicationCommands.Delete" Header="Delete" Style="{StaticResource ThemedMenuItem}">
                                <MenuItem.Icon>
                                    <Border Background="#ef4444" CornerRadius="3" Width="16" Height="16">
                                        <ui:FontIcon Glyph="&#xE74D;" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </TextBox.ContextMenu>
                </TextBox>

                <Button Grid.Column="1" 
                        Content="Send" 
                        Command="{Binding SendConsoleCommand}" 
                        Style="{StaticResource {x:Type Button}}"
                        Margin="0,0,8,0"/>

                <ToggleButton Grid.Column="2" 
                              IsChecked="{Binding IsConsoleConnected}" 
                              Command="{Binding ToggleConsoleConnectionCommand}"
                              Style="{StaticResource ModernToggleButton}">
                    <ToggleButton.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="●" Margin="0,0,6,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#ef4444"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConsoleConnected}" Value="True">
                                                <Setter Property="Foreground" Value="#22c55e"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Connect"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConsoleConnected}" Value="True">
                                                <Setter Property="Text" Value="Disconnect"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </ToggleButton.Content>
                </ToggleButton>
                
                <ToggleButton Grid.Column="3" 
                              IsChecked="{Binding ShowUserCommandsOnly}"
                              Content="Filter"
                              Style="{StaticResource ModernToggleButton}"
                              Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
